#You can get a list of the available VM images using the following command.These are popular os images 
az vm image list --output table
#You can get a full list by adding the --all flag to the command.From azure marketplace
#Since the list of images in the Marketplace is very large, it is helpful to filter the list with the --publisher, --sku or –-offer options.
az vm image list --sku Wordpress --output table --all
az vm image list --publisher Microsoft --output table --all
